#                🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨
#           This file was automatically generated from src/transformers/models/qwen3/modular_qwen3.py.
#               Do NOT edit this file manually as any edits will be overwritten by the generation of
#             the file from the modular. If any change should be done, please apply the change to the
#                          modular_qwen3.py file directly. One of our CI enforces this.
#                🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨
# Copyright 2025 The Qwen team, Alibaba Group and the HuggingFace Inc. team. All rights reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
"""A small, self-contained PyTorch implementation of Qwen3.

Key architecture comments retain the intent of the Hugging Face implementation.
"""

import math
import sys
from pathlib import Path

import torch
from torch import nn
from torch.nn import functional as F

try:
    from ..utils import (
        KVCache,
        Qwen3CausalLMOutput,
        Qwen3ModelOutput,
        RotaryEmbedding,
        apply_standard_rotary_pos_emb,
        build_attention_mask,
        repeat_kv,
    )
    from .configuration_qwen3 import Qwen3Config
except ImportError:
    sys.path.append(str(Path(__file__).resolve().parent.parent))
    from configuration_qwen3 import Qwen3Config
    from utils import (
        KVCache,
        Qwen3CausalLMOutput,
        Qwen3ModelOutput,
        RotaryEmbedding,
        apply_standard_rotary_pos_emb,
        build_attention_mask,
        repeat_kv,
    )


class Qwen3RMSNorm(nn.Module):
    """Root-mean-square normalization used by Qwen3."""

    def __init__(self, hidden_size: int, eps: float = 1e-6) -> None:
        """Initialize a learnable RMS scale.

        Parameters:
            hidden_size: Size of the hidden-state final dimension.
            eps: Numerical stabilizer added to the RMS variance.
        """
        super().__init__()
        self.weight = nn.Parameter(torch.ones(hidden_size))
        self.variance_epsilon = eps

    def forward(self, hidden_states: torch.Tensor) -> torch.Tensor:
        """Normalize hidden states in float32 and restore input dtype.

        Parameters:
            hidden_states: Tensor whose final dimension equals the norm size.
        """
        input_dtype = hidden_states.dtype
        normalized_states = hidden_states.float()
        variance = normalized_states.pow(2).mean(dim = -1, keepdim = True)
        normalized_states = normalized_states * torch.rsqrt(variance + self.variance_epsilon)
        return (self.weight * normalized_states).to(dtype = input_dtype)


class Qwen3MLP(nn.Module):
    """Qwen3 gated feed-forward network inherited from the Gemma-style MLP."""

    def __init__(self, config: Qwen3Config) -> None:
        """Create bias-free gate, up, and down projections.

        Parameters:
            config: Model configuration defining hidden and intermediate sizes.
        """
        super().__init__()
        self.gate_proj = nn.Linear(config.hidden_size, config.intermediate_size, bias = False)
        self.up_proj = nn.Linear(config.hidden_size, config.intermediate_size, bias = False)
        self.down_proj = nn.Linear(config.intermediate_size, config.hidden_size, bias = False)

    def forward(self, hidden_states: torch.Tensor) -> torch.Tensor:
        """Apply the SiLU-gated Qwen3 feed-forward transformation.

        Parameters:
            hidden_states: Input tensor shaped ``[batch, tokens, hidden_size]``.
        """
        return self.down_proj(F.silu(self.gate_proj(hidden_states)) * self.up_proj(hidden_states))


class Qwen3Attention(nn.Module):
    """Grouped-query causal attention with per-head Q/K normalization."""

    def __init__(self, config: Qwen3Config, layer_idx: int) -> None:
        """Create projections, Q/K head norms, and cache metadata.

        Parameters:
            config: Model configuration used to size attention projections.
            layer_idx: Decoder-layer index used to address a KV cache.
        """
        super().__init__()
        self.config = config
        self.layer_idx = layer_idx
        self.head_dim = config.head_dim
        self.num_attention_heads = config.num_attention_heads
        self.num_key_value_heads = config.num_key_value_heads
        self.num_key_value_groups = self.num_attention_heads // self.num_key_value_heads
        self.scaling = self.head_dim**-0.5
        self.layer_type = config.layer_types[layer_idx]
        self.sliding_window = config.sliding_window if self.layer_type == "sliding_attention" else None
        self.q_proj = nn.Linear(
            config.hidden_size,
            self.num_attention_heads * self.head_dim,
            bias = config.attention_bias,
        )
        self.k_proj = nn.Linear(
            config.hidden_size,
            self.num_key_value_heads * self.head_dim,
            bias = config.attention_bias,
        )
        self.v_proj = nn.Linear(
            config.hidden_size,
            self.num_key_value_heads * self.head_dim,
            bias = config.attention_bias,
        )
        self.o_proj = nn.Linear(
            self.num_attention_heads * self.head_dim,
            config.hidden_size,
            bias = config.attention_bias,
        )
        # 原版关键注释 / Unlike OLMo, normalization is only on the head dimension.
        self.q_norm = Qwen3RMSNorm(self.head_dim, eps = config.rms_norm_eps)
        self.k_norm = Qwen3RMSNorm(self.head_dim, eps = config.rms_norm_eps)

    def forward(
        self,
        hidden_states: torch.Tensor,
        position_embeddings: tuple[torch.Tensor, torch.Tensor],
        attention_mask: torch.Tensor | None,
        past_key_values: KVCache | None,
        use_cache: bool,
    ) -> torch.Tensor:
        """Compute causal GQA output for one Qwen3 decoder layer.

        Parameters:
            hidden_states: Input states shaped ``[batch, tokens, hidden_size]``.
            position_embeddings: Standard RoPE cosine and sine tensors.
            attention_mask: Optional 1/0 mask over cached and current key tokens.
            past_key_values: Optional per-layer KV cache.
            use_cache: Whether current K/V states are appended to the cache.
        """
        batch_size, query_length, _ = hidden_states.shape
        projection_shape = (batch_size, query_length, -1, self.head_dim)
        query_states = self.q_norm(self.q_proj(hidden_states).view(projection_shape)).transpose(1, 2)
        key_states = self.k_norm(self.k_proj(hidden_states).view(projection_shape)).transpose(1, 2)
        value_states = self.v_proj(hidden_states).view(projection_shape).transpose(1, 2)
        cos, sin = position_embeddings
        query_states, key_states = apply_standard_rotary_pos_emb(query_states, key_states, cos, sin)

        past_length = 0
        if past_key_values is not None:
            past_length = past_key_values.get_seq_length(self.layer_idx)
            if use_cache:
                key_states, value_states = past_key_values.update(key_states, value_states, self.layer_idx)

        key_states = repeat_kv(key_states, self.num_key_value_groups)
        value_states = repeat_kv(value_states, self.num_key_value_groups)
        additive_mask = build_attention_mask(
            attention_mask = attention_mask,
            query_length = query_length,
            key_length = key_states.shape[-2],
            past_length = past_length,
            layer_type = self.layer_type,
            sliding_window = self.sliding_window,
            device = hidden_states.device,
            dtype = query_states.dtype,
        )
        attention_logits = torch.matmul(query_states, key_states.transpose(-2, -1)) * self.scaling
        attention_logits = attention_logits + additive_mask
        attention_weights = F.softmax(attention_logits, dim = -1, dtype = torch.float32).to(dtype = query_states.dtype)
        attention_weights = F.dropout(
            attention_weights,
            p = self.config.attention_dropout if self.training else 0.0,
            training = self.training,
        )
        attention_output = torch.matmul(attention_weights, value_states)
        attention_output = attention_output.transpose(1, 2).reshape(batch_size, query_length, -1).contiguous()
        return self.o_proj(attention_output)


class Qwen3DecoderLayer(nn.Module):
    """One pre-norm Qwen3 attention and feed-forward decoder block."""

    def __init__(self, config: Qwen3Config, layer_idx: int) -> None:
        """Create one Qwen3 attention, MLP, and normalization layer.

        Parameters:
            config: Model configuration used by this decoder layer.
            layer_idx: Decoder-layer index used by attention cache lookup.
        """
        super().__init__()
        self.self_attn = Qwen3Attention(config, layer_idx)
        self.mlp = Qwen3MLP(config)
        self.input_layernorm = Qwen3RMSNorm(config.hidden_size, eps = config.rms_norm_eps)
        self.post_attention_layernorm = Qwen3RMSNorm(config.hidden_size, eps = config.rms_norm_eps)

    def forward(
        self,
        hidden_states: torch.Tensor,
        position_embeddings: tuple[torch.Tensor, torch.Tensor],
        attention_mask: torch.Tensor | None,
        past_key_values: KVCache | None,
        use_cache: bool,
    ) -> torch.Tensor:
        """Apply the Qwen3 attention and MLP residual sublayers.

        Parameters:
            hidden_states: Input tensor shaped ``[batch, tokens, hidden_size]``.
            position_embeddings: Standard RoPE cosine and sine tensors.
            attention_mask: Optional valid-token mask for attention keys.
            past_key_values: Optional cache holding past attention K/V tensors.
            use_cache: Whether current K/V tensors are appended to the cache.
        """
        residual = hidden_states
        normalized_states = self.input_layernorm(hidden_states)
        # Self Attention / 自注意力。
        attention_output = self.self_attn(
            normalized_states,
            position_embeddings = position_embeddings,
            attention_mask = attention_mask,
            past_key_values = past_key_values,
            use_cache = use_cache,
        )
        hidden_states = residual + attention_output

        residual = hidden_states
        normalized_states = self.post_attention_layernorm(hidden_states)
        # Fully Connected / 前馈网络。
        hidden_states = residual + self.mlp(normalized_states)
        return hidden_states


class Qwen3Model(nn.Module):
    """Qwen3 decoder stack without a language-model projection head."""

    def __init__(self, config: Qwen3Config) -> None:
        """Create embeddings, decoder layers, RoPE, and final normalization.

        Parameters:
            config: Model configuration defining the decoder stack.
        """
        super().__init__()
        self.config = config
        self.embed_tokens = nn.Embedding(config.vocab_size, config.hidden_size, config.pad_token_id)
        self.layers = nn.ModuleList(
            [Qwen3DecoderLayer(config, layer_idx) for layer_idx in range(config.num_hidden_layers)]
        )
        self.norm = Qwen3RMSNorm(config.hidden_size, eps = config.rms_norm_eps)
        self.rotary_emb = RotaryEmbedding(config)
        self.apply(self._init_weights)

    def _init_weights(self, module: nn.Module) -> None:
        """Initialize embeddings and projections with Qwen3's normal convention.

        Parameters:
            module: Child module visited by ``nn.Module.apply``.
        """
        if isinstance(module, nn.Linear):
            nn.init.normal_(module.weight, mean = 0.0, std = self.config.initializer_range)
            if module.bias is not None:
                nn.init.zeros_(module.bias)
        elif isinstance(module, nn.Embedding):
            nn.init.normal_(module.weight, mean = 0.0, std = self.config.initializer_range)
            if module.padding_idx is not None:
                module.weight.data[module.padding_idx].zero_()

    def forward(
        self,
        input_ids: torch.Tensor | None = None,
        attention_mask: torch.Tensor | None = None,
        position_ids: torch.Tensor | None = None,
        past_key_values: KVCache | None = None,
        inputs_embeds: torch.Tensor | None = None,
        use_cache: bool | None = None,
    ) -> Qwen3ModelOutput:
        """Embed tokens and run the Qwen3 decoder stack.

        Parameters:
            input_ids: Token IDs shaped ``[batch, tokens]``.
            attention_mask: Optional 1/0 mask over cached and current tokens.
            position_ids: Optional absolute positions shaped ``[batch, tokens]``.
            past_key_values: Optional cache from a previous decoding call.
            inputs_embeds: Optional precomputed embeddings instead of token IDs.
            use_cache: Whether to create or append to a KV cache.
        """
        if (input_ids is None) == (inputs_embeds is None):
            raise ValueError("Specify exactly one of input_ids or inputs_embeds")
        use_cache = self.config.use_cache if use_cache is None else use_cache
        if inputs_embeds is None:
            inputs_embeds = self.embed_tokens(input_ids)
        if use_cache and past_key_values is None:
            past_key_values = KVCache()

        past_length = past_key_values.get_seq_length() if past_key_values is not None else 0
        if position_ids is None:
            position_ids = torch.arange(
                past_length,
                past_length + inputs_embeds.shape[1],
                device = inputs_embeds.device,
            ).unsqueeze(0)
        if position_ids.shape[0] == 1 and inputs_embeds.shape[0] > 1:
            position_ids = position_ids.expand(inputs_embeds.shape[0], -1)

        hidden_states = inputs_embeds
        position_embeddings = self.rotary_emb(hidden_states, position_ids)
        for decoder_layer in self.layers:
            hidden_states = decoder_layer(
                hidden_states,
                position_embeddings = position_embeddings,
                attention_mask = attention_mask,
                past_key_values = past_key_values,
                use_cache = use_cache,
            )
        return Qwen3ModelOutput(
            last_hidden_state = self.norm(hidden_states),
            past_key_values = past_key_values if use_cache else None,
        )


class Qwen3ForCausalLM(nn.Module):
    """Qwen3 decoder with a causal language-model projection head."""

    def __init__(self, config: Qwen3Config) -> None:
        """Create the Qwen3 decoder and vocabulary projection head.

        Parameters:
            config: Model configuration used by the decoder and output head.
        """
        super().__init__()
        self.config = config
        self.model = Qwen3Model(config)
        self.lm_head = nn.Linear(config.hidden_size, config.vocab_size, bias = False)
        self.model._init_weights(self.lm_head)
        if config.tie_word_embeddings:
            self.lm_head.weight = self.model.embed_tokens.weight

    def forward(
        self,
        input_ids: torch.Tensor,
        attention_mask: torch.Tensor | None = None,
        past_key_values: KVCache | None = None,
        use_cache: bool | None = None,
        labels: torch.Tensor | None = None,
    ) -> Qwen3CausalLMOutput:
        """Produce Qwen3 causal logits and optional shifted cross-entropy loss.

        Parameters:
            input_ids: Token IDs shaped ``[batch, tokens]``.
            attention_mask: Optional 1/0 valid-token mask.
            past_key_values: Optional cache returned by an earlier forward call.
            use_cache: Whether to return and update a per-layer KV cache.
            labels: Optional target IDs; ``-100`` entries are ignored by cross entropy.
        """
        model_outputs = self.model(
            input_ids = input_ids,
            attention_mask = attention_mask,
            past_key_values = past_key_values,
            use_cache = use_cache,
        )
        logits = self.lm_head(model_outputs.last_hidden_state)
        loss = None
        if labels is not None:
            if labels.shape != input_ids.shape:
                raise ValueError("labels must have the same shape as input_ids")
            if input_ids.shape[1] < 2:
                loss = logits.sum() * 0.0
            else:
                loss = F.cross_entropy(
                    logits[:, :-1, :].reshape(-1, self.config.vocab_size),
                    labels[:, 1:].reshape(-1),
                    ignore_index = -100,
                )
        return Qwen3CausalLMOutput(
            logits = logits,
            loss = loss,
            past_key_values = model_outputs.past_key_values,
        )
